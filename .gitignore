# Secrets — never commit API keys
*.env
.env
.env.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Runtime artifacts / user data
chat_history.json
thinking_temp.json
ai_thinking_log.json
# Seed docs are tracked; large/private user files can be added here later
!knowledge_sources/*.md

# Build
build/
dist/
*.spec

# OS / editor
.DS_Store
.vscode/
.idea/
.vercel

# Local AI-agent tooling (per-developer, not app code) -- .mcp.json is
# intentionally NOT here: it's the project-scoped MCP server config, meant
# to be shared with the team.
.claude/
.agents/
skills-lock.json

# Node / Next.js
node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
vercel-deployment/.next/
vercel-deployment/node_modules/

# Trained model artifacts (reproducible via python -m train.train)
models/
